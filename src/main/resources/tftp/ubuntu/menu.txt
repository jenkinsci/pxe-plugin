menu hshift 13
menu width 49
menu margin 8

MENU TITLE ${RELEASE}

INCLUDE ubuntu/stdmenu.cfg

label install
	menu label Interactive Install
	kernel ${ID}/linux
	append vga=normal initrd=${ID}/initrd.gz -- quiet

label autoinstall
	menu label Automatic Install for Hudson
	kernel ${ID}/linux
	append vga=normal initrd=${ID}/initrd.gz auto url=${PRESEEDURL} locale=${LOCALE} debian-installer/keymap=us -- quiet

label mainmenu
    menu label ^Back to main menu
    kernel vesamenu.c32
